meta:
  environment: python
  toolchain:
    name: pip
    version: "3.12"

build:
  requirementsPath: requirements.txt
  useCache: true

run:
  scriptName: bot.py
  persistenceMount: /data
  containerPort: 8080
